# CIHub Interactive CLI

This package provides the TypeScript/Ink interactive CLI for CIHub.

Status: scaffolding only. The CLI checks that the Python `cihub` CLI is
available and reports version mismatches before starting the UI.
