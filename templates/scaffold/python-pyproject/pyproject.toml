[project]
name = "cihub_pyproject_sample"
version = "0.1.0"
requires-python = ">=3.11"

dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 88

[tool.black]
line-length = 88

[tool.isort]
profile = "black"
