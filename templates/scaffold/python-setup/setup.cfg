# CI/CD Hub Scaffold - Python with setup.py
# This setup.cfg provides pytest configuration for the project

[tool:pytest]
testpaths = tests
pythonpath = .

[tool:ruff]
line-length = 88

[tool:black]
line-length = 88

[tool:isort]
profile = black
